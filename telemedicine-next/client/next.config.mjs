/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Recommended for better error handling
    // Add any custom configuration if needed
  };
  
  export default nextConfig;
  